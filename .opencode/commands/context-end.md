---
description: Close a Context OS session through its review-gated workflow
---

Use the `skill` tool to load the exact skill named `context-end`, then follow
the returned instructions exactly. Do not substitute another skill or reproduce
the workflow from memory. Treat any text after this command as user input:
$ARGUMENTS
