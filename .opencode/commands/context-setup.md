---
description: Set up Context OS through its review-gated lifecycle workflow
---

Use the `skill` tool to load the exact skill named `context-setup`, then follow
the returned instructions exactly. Do not substitute another skill or reproduce
the workflow from memory. Treat any text after this command as user input:
$ARGUMENTS
