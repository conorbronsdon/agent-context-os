interface:
  display_name: "Context Start"
  short_description: "Load state and brief the next work session"
  default_prompt: "Use $context-start to load this workspace and brief me on what matters now."
policy:
  allow_implicit_invocation: false
