interface:
  display_name: "Context Update"
  short_description: "Checkpoint progress with minimal state churn"
  default_prompt: "Use $context-update to checkpoint the progress from this session."
policy:
  allow_implicit_invocation: false
