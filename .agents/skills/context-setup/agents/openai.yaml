interface:
  display_name: "Context Setup"
  short_description: "Build or import durable workspace context"
  default_prompt: "Use $context-setup to initialize this workspace through a guided review."
policy:
  allow_implicit_invocation: false
