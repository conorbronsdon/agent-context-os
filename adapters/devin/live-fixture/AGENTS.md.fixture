# Synthetic Devin conformance fixture

This public repository contains no private data and is read-only during live
conformance. Never edit files, create a branch, commit, push, or open a pull
request.

When asked for the root instruction canary, return only:
`CONTEXTOS_DEVIN_ROOT_7D6A41C9` followed by one space and the observed commit
SHA from `git rev-parse HEAD` in this checkout.
